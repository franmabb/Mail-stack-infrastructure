# Mail-stack
Comprehensive Linux mail server stack deployment on Ubuntu. Features automated configuration for Bind9 (DNS), Postfix (SMTP), and Dovecot (IMAP/POP3), including secure Maildir implementation and custom DNS zones.
